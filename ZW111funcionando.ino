/*
 * Este arquivo existe apenas para que o Arduino IDE reconheça a pasta
 * como um sketch válido. Todo o código principal está em main.cpp.
 * O Arduino IDE compilará todos os arquivos .cpp/.h na pasta junto com este
 * arquivo.
 */
